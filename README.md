## Course Application Form (Formik + Yup)

Run locally:

```bash
cd app
npm install
npm run dev
```

What’s included:
- React + Vite scaffold
- Formik form with Yup validation

Behavior:
- Required fields show red error text under the relevant field
- On success: `alert(JSON.stringify(values, null, 2))`
